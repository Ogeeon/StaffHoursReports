This is a sister utility for StaffHoursClient - tracking hours worked by developers of a small team.
The purpose of this one is to load tasks data from Excel files and generate reports.

#### Creating distribution
Use `gradlew buildFatJar` to create in the project's root directory a jar file - the end user's software.